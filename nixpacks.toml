[phases.setup]
nixPkgs = ["python39", "nodejs-18_x", "postgresql"]

[phases.install]
cmds = [
    "cd tailorcv-backend",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "cd tailorcv-backend && python app.py"