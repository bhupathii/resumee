{
  "name": "tailorcv",
  "version": "1.0.0",
  "scripts": {
    "build": "cd tailorcv-frontend && npm install && npm run build",
    "start": "cd tailorcv-frontend && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}